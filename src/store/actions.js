// root actions
import * as types from './mutations';

/* eslint-disable */
// export const markAsFavorite = ({ commit }, quote) => {
//   if (!quote.is_favorite) {
//     commit(types.MARK_AS_FAVORITE, {
//       id: quote.id,
//     });
//   }
// };
/* eslint-enable */
